[project]
name = "openclaw-canvas"
version = "0.1.0"
description = "Unified spatial dashboard for OpenClaw"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "python-multipart>=0.0.9",
]
